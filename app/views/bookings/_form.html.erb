<%= form_for [@lodging, @booking], remote: true do |f| %>
  <%= f.hidden_field :user_id, value: @user.id %>
  <%= f.label :start_date %>
  <%= f.date_select :start_date %>
  <%= f.label :end_date %>
  <%= f.date_select :end_date %>
  <%= f.submit "Submit", class: 'btn btn-success' %>
<% end %>
